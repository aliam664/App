<div dir="rtl">

# راهنمای مدیر — قرار دادن پک گرافیکی و انتشار نسخه

این سند برای کسی است که **محتوای پک** را آماده می‌کند و **فایل نصبی (EXE)** را منتشر می‌کند. هیچ دانش برنامه‌نویسی لازم نیست؛ فقط مراحل را به ترتیب انجام دهید.

> [!IMPORTANT]
> همهٔ قوانین این سند مستقیماً از کدِ نصب‌کننده (`src-tauri/src/installer.rs` و `catalog.rs`) استخراج شده‌اند. اگر خلاف آن‌ها عمل کنید، برنامه یا فایل را نصب نمی‌کند یا آن را «موجود نیست» گزارش می‌دهد.

---

## فهرست

1. [یک قانون طلایی](#۱-یک-قانون-طلایی)
2. [ساختار پوشهٔ `mods/`](#۲-ساختار-پوشهٔ-mods)
3. [قرار دادن پک گرافیکی (۵ سطح)](#۳-قرار-دادن-پک-گرافیکی-۵-سطح)
4. [افزودن افزونه (Add-on)](#۴-افزودن-افزونه-add-on)
5. [ساخت و انتشار EXE از طریق GitHub](#۵-ساخت-و-انتشار-exe-از-طریق-github)
6. [چک‌لیست پیش از انتشار](#۶-چکلیست-پیش-از-انتشار)
7. [رفتار برنامه روی سیستم کاربر](#۷-رفتار-برنامه-روی-سیستم-کاربر)
8. [پرسش‌های متداول](#۸-پرسشهای-متداول)

---

## ۱. یک قانون طلایی

> **هر پوشهٔ منبع، آینهٔ پوشهٔ بازی است و عیناً روی ریشهٔ Assetto Corsa کپی می‌شود.**

`<AC>` یعنی پوشهٔ بازی، معمولاً:
`C:\Program Files (x86)\Steam\steamapps\common\assettocorsa`

| اگر این فایل را بگذارید | این‌جا نصب می‌شود |
|---|---|
| `mods/graphics/ultra/dwrite.dll` | `<AC>\dwrite.dll` |
| `mods/graphics/ultra/extension/config/general.ini` | `<AC>\extension\config\general.ini` |
| `mods/graphics/ultra/system/cfg/video.ini` | `<AC>\system\cfg\video.ini` |
| `mods/graphics/ultra/system/cfg/ppfilters/UHM.ini` | `<AC>\system\cfg\ppfilters\UHM.ini` |
| `mods/addons/gas-hud/files/apps/python/GasHUD/GasHUD.py` | `<AC>\apps\python\GasHUD\GasHUD.py` |

فایل‌های فشرده (ZIP/RAR) داخل `mods/` **نصب نمی‌شوند** — همیشه محتوای اکسترکت‌شده را بگذارید.

---

## ۲. ساختار پوشهٔ `mods/`

```text
mods/
├── graphics/                 ← پک گرافیکی (یک واحد نصب، کاربر یک سطح انتخاب می‌کند)
│   ├── common/               ← اختیاری: فایل‌های مشترک همهٔ سطح‌ها (اول کپی می‌شود)
│   ├── low/                  ← سطح ۱
│   ├── medium/               ← سطح ۲
│   ├── high/                 ← سطح ۳
│   ├── veryhigh/             ← سطح ۴
│   └── ultra/                ← سطح ۵
└── addons/                   ← افزونه‌های اختیاری (هر تعداد)
    ├── _example-addon/       ← الگو — چون با «_» شروع می‌شود در برنامه مخفی است
    └── <addon-id>/
        ├── mod.json          ← اختیاری: نام، توضیح، نسخه، …
        ├── preview.png       ← تصویر کارت
        └── files/            ← آینهٔ پوشهٔ بازی
```

**نام پوشه‌ها دقیقاً و با حروف کوچک:** `common` `low` `medium` `high` `veryhigh` `ultra` (`veryhigh` سرهم). هر نام دیگری نادیده گرفته می‌شود.

---

## ۳. قرار دادن پک گرافیکی (۵ سطح)

### گام ۱ — آماده‌سازی هر سطح روی کامپیوتر خودتان
1. پک هر سطح را در یک پوشهٔ موقت **اکسترکت** کنید.
2. با یک نصب سالم AC مقایسه کنید تا بدانید هر فایل نسبت به ریشهٔ بازی کجا می‌نشیند.
3. مطمئن شوید هیچ پوشهٔ اضافه‌ای مثل `UHM Ultra v3\` بالای ساختار نیست؛ اولین سطح داخل پوشهٔ سطح باید همان چیزهایی باشد که مستقیم در `<AC>` قرار می‌گیرد (`dwrite.dll`، `extension\`، `system\`، `apps\` …).

### گام ۲ — کپی به مخزن
محتوای هر سطح را در پوشهٔ متناظر بریزید. نمونهٔ کامل برای Ultra:

```text
mods/graphics/ultra/
├── dwrite.dll                              ← CSP
├── extension/
│   ├── config/                             ← تنظیمات CSP
│   ├── config-ext/pure/                    ← Pure + SRP
│   ├── lua/
│   └── weather/
├── system/cfg/
│   ├── video.ini                           ← تنظیمات ویدیو
│   ├── camera_onboard_free.ini             ← Chase Cam
│   └── ppfilters/UHM_Ultra.ini             ← PP Filter
└── apps/python/UHM_HUD/                    ← HUD
```

همین کار را برای `low/` `medium/` `high/` `veryhigh/` تکرار کنید.

### گام ۳ — (اختیاری) استفاده از `common/` برای حذف تکرار
اگر بخشی از پک (مثلاً کل CSP یا Pure) در هر ۵ سطح **یکسان** است، یک‌بار در `mods/graphics/common/` بگذارید و در پوشهٔ هر سطح فقط فایل‌های متفاوت (`video.ini`، فیلتر PP و …) را نگه دارید.

**ترتیب نصب:** اول `common/` سپس `<سطح>/`. **فایل هم‌نام در پوشهٔ سطح همیشه برنده است.**

### قوانینی که برنامه اعمال می‌کند

| موضوع | رفتار |
|---|---|
| سطح خالی | اگر پوشهٔ سطح انتخابی و `common/` هر دو خالی باشند → وضعیت «فایل موجود نیست» (بدون خطا، بدون نصب) |
| فایل‌های نادیده‌گرفته‌شده | `.gitkeep` `Thumbs.db` `desktop.ini` `.DS_Store` هرگز کپی نمی‌شوند |
| بکاپ | پیش از بازنویسی هر فایل موجود، **فقط بار اول** نسخهٔ اصلی کاربر در `%APPDATA%\com.uhm.packinstaller\backups\` ذخیره می‌شود؛ تعویض سطح بعدی بکاپ اصلی را خراب نمی‌کند |
| «CSP / Pure فعلی‌ام را نگه دار» | اگر کاربر این گزینه را در مرحلهٔ بررسی انتخاب کند، مسیرهای CSP (`dwrite.dll`، `extension/config`، `extension/lua` …) یا Pure (`extension/config-ext/pure` …) از کپی حذف می‌شوند |
| حذف | «مدیریت مودها ← حذف» همهٔ فایل‌های نصب‌شده را برمی‌دارد، بکاپ‌ها را برمی‌گرداند و پوشه‌های خالی را (فقط زیر ریشهٔ بازی) پاک می‌کند |

### چیپ‌های روی کارت هر سطح
متن‌های «CSP · PURE · PP …» روی کارت‌های سطح فقط **نمایشی** هستند و از جدول `TIER_MODS` در `src/js/modConfig.js` می‌آیند. اگر محتوای واقعی سطح‌هایتان متفاوت است، آن جدول را ویرایش کنید. تصاویر کارت‌ها در `src/assets/images/tiers/*.webp` هستند.

---

## ۴. افزودن افزونه (Add-on)

1. پوشهٔ `mods/addons/_example-addon/` را کپی کنید و نام جدیدی بدهید، مثلاً `gas-hud`
   (فقط حروف انگلیسی، عدد، `-` `_` `.` — بدون فاصله؛ نام‌هایی که با `_` یا `.` شروع شوند مخفی‌اند).
2. فایل‌ها را با ساختار آینهٔ بازی داخل `files/` بریزید.
3. یک تصویر `preview.png` (یا `.jpg` / `.webp`؛ ترجیحاً ۱۶:۹، مثلاً ۱۲۸۰×۷۲۰) کنار آن بگذارید.
4. `mod.json` را ویرایش کنید (اختیاری؛ اگر نباشد نام پوشه نمایش داده می‌شود: `gas-hud` ← «Gas Hud»).

```json
{
  "name":        { "fa": "نمایشگر گاز", "en": "Gas HUD" },
  "description": { "fa": "نمایش زندهٔ گاز و ترمز", "en": "Live throttle & brake HUD" },
  "version": "1.2",
  "author": "UHM",
  "category": "hud",
  "requires": ["csp"],
  "recommendedTiers": ["high", "veryhigh", "ultra"],
  "order": 10,
  "preview": "preview.png"
}
```

| فیلد | نوع | نمایش در برنامه |
|---|---|---|
| `name` | متن یا `{fa, en}` | عنوان کارت |
| `description` | متن یا `{fa, en}` | توضیح (حداکثر ۳ خط) |
| `version` | متن / عدد | چیپ `v1.2` |
| `author` | متن | «سازنده: …» |
| `category` | متن | چیپ دسته‌بندی (hud، weather، sound، camera …) |
| `requires` | لیست | چیپ هشدار «نیازمند csp» — فقط اطلاع‌رسانی |
| `recommendedTiers` | لیست از ۵ سطح | چیپ سطح‌های پیشنهادی |
| `order` | عدد | ترتیب نمایش (کوچک‌تر = بالاتر؛ پیش‌فرض ۱۰۰۰) |
| `preview` | مسیر نسبی | اگر تصویر نام دیگری دارد |

`mod.json` و `preview.png` **هرگز** داخل بازی کپی نمی‌شوند. افزونهٔ بدون فایل نمایش داده می‌شود ولی دکمهٔ نصبش غیرفعال است.

---

## ۵. ساخت و انتشار EXE از طریق GitHub

نیازی به نصب Rust یا Node روی کامپیوتر خودتان نیست؛ GitHub Actions همه‌چیز را می‌سازد.

### ۵.۱ — بارگذاری محتوا
1. مخزن را باز کنید: `https://github.com/aliam664/App`
2. به `mods/graphics/<سطح>/` بروید ← **Add file ← Upload files** ← پوشه‌ها را بکشید و رها کنید.
   (برای حجم بالا، `git` روی کامپیوتر راحت‌تر است: `git add mods && git commit -m "Add pack" && git push`)
3. پس از هر push، تب **Actions** یک اجرای «Build & Test» نشان می‌دهد. باید **سبز** شود (~۱۵ دقیقه).

> [!TIP]
> اگر مجموع `mods/` از ~۵۰ مگابایت بیشتر شد، از Git LFS استفاده کنید:
> `git lfs install && git lfs track "mods/**" && git add .gitattributes`

### ۵.۲ — تعیین شمارهٔ نسخه
شمارهٔ نسخه در **سه فایل** باید یکسان باشد:
`package.json` · `src-tauri/tauri.conf.json` · `src-tauri/Cargo.toml` (فیلد `version`).
مثلاً `1.0.0` ← `1.1.0`.

### ۵.۳ — انتشار
1. **Releases ← Draft a new release**
2. **Choose a tag** ← بنویسید `v1.1.0` (حتماً با `v` کوچک و هم‌نام با نسخهٔ بالا) ← **Create new tag on publish**
3. **Target:** برنچ اصلی
4. عنوان: `UHM Pack Installer 1.1.0`؛ توضیحات: تغییرات این نسخه
5. **Publish release**

با انتشار تگ، Actions به‌طور خودکار:
- تست‌ها را اجرا می‌کند،
- EXE ویندوز را می‌سازد،
- فایل `UHM.Pack.Installer_1.1.0_x64-setup.exe` و `SHA256SUMS.txt` را به همان Release می‌چسباند.

~۱۵ دقیقه بعد صفحهٔ Release را رفرش کنید؛ فایل در بخش **Assets** است.

### ۵.۴ — اگر بیلد قرمز شد
یک Issue خودکار با عنوان `CI ❌ …` ساخته می‌شود که خلاصهٔ خطا را دارد. آن را برای توسعه‌دهنده بفرستید یا در همان چت پشتیبانی ذکر کنید.

---

## ۶. چک‌لیست پیش از انتشار

- [ ] `mods/graphics/low|medium|high|veryhigh|ultra/` هر کدام محتوای اکسترکت‌شدهٔ همان سطح را دارند
- [ ] هیچ ZIP/RAR داخل `mods/` نیست
- [ ] ساختار داخل هر پوشه = ساختار پوشهٔ بازی (`system/cfg/…`، `extension/…`، `apps/…`)
- [ ] هیچ پوشهٔ wrapper اضافه (مثل `UHM Ultra v3\`) بالای ساختار نیست
- [ ] هر افزونه: `files/` پر + `preview.png` + `mod.json` معتبر (JSON بدون ویرگول اضافه)
- [ ] شمارهٔ نسخه در ۳ فایل یکسان است و تگ Release با آن مطابقت دارد
- [ ] آخرین اجرای Actions روی برنچ **سبز** است
- [ ] EXE را روی یک **کپی** از پوشهٔ بازی امتحان کرده‌اید: نصب یک سطح، تعویض سطح، حذف کامل

---

## ۷. رفتار برنامه روی سیستم کاربر

| چه | کجا |
|---|---|
| تنظیمات | `%APPDATA%\com.uhm.packinstaller\settings.json` |
| فهرست نصب‌شده‌ها | `%APPDATA%\com.uhm.packinstaller\manifest.json` |
| بکاپ فایل‌های اصلی | `%APPDATA%\com.uhm.packinstaller\backups\` |
| سطل بازیافت کتابخانه | `%APPDATA%\com.uhm.packinstaller\trash\` |
| محتوای باندل‌شده | کنار برنامه در `resources\mods\` (فقط‌خواندنی) |

برنامه **هرگز** خارج از پوشهٔ بازی و پوشهٔ داده‌های خودش چیزی نمی‌نویسد. حذف برنامه از ویندوز، پوشهٔ بازی را دست نمی‌زند؛ برای برگرداندن فایل‌های اصلی، قبل از حذف برنامه از «مدیریت مودها ← حذف» استفاده کنید.

---

## ۸. پرسش‌های متداول

**کاربر سطح را عوض می‌کند؛ فایل‌های سطح قبلی چه می‌شوند؟**
فایل‌های هم‌نام بازنویسی می‌شوند؛ بکاپ اصلی کاربر (از اولین نصب) دست‌نخورده می‌ماند. برای برگشت کامل به حالت قبل از UHM، «مدیریت مودها ← حذف».

**می‌خواهم افزونه‌ای فقط در یک زیرپوشهٔ خاص نصب شود.**
همان مسیر را داخل `files/` بسازید: `files/system/cfg/ppfilters/X.ini`.

**می‌توانم رابط را بدون نصب برنامه ببینم؟**
بله — `view/index.html` را در Chrome باز کنید. این پوشه کپی دقیق رابط داخل برنامه است و به‌طور خودکار هم‌گام نگه داشته می‌شود؛ آن را دستی ویرایش نکنید.

**SmartScreen هشدار می‌دهد.**
طبیعی است؛ فایل امضای دیجیتال ندارد. **More info ← Run anyway**. برای حذف هشدار باید گواهی Code Signing خریداری و به workflow اضافه شود.

</div>
