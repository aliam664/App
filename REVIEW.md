# 🔍 بازبینی کامل پروژه UHM Pack Installer

> تاریخ بازبینی: 2026-09-10
> شاخه: `arena/01a0884d-app` (آخرین: `71327cc`)
> نتیجه‌ی `npm run check`: ✅ پاس
>
> ✅ **به‌روزرسانی:** همه‌ی ۵ مورد بحرانی این بازبینی (فایل‌های مود، overwrite، ترتیب مانیفست، حالت پایان، مسیر asar) رفع شدند و به ریموت پوش شدند. جزئیات در بخش «۰.۱» آمده است.

---

## ۰.۱ وضعیت پس از رفع (Finalization)

| مورد | وضعیت |
|---|---|
| `src/assets/mod-files/*` در گیت | ✅ `.gitkeep` + `README.md` اضافه شد |
| تصمیم overwrite CSP/PURE | ✅ در `tierSelect` مود حذف و در `installer` وضعیت `skipped` |
| ترتیب مانیفست در Manage | ✅ `entries[0]` به‌عنوان جدیدترین |
| صفحه‌ی پایان (انصراف/خطا/ناقص) | ✅ حالت‌های جدا با آمار کامل |
| مسیر asar/unpacked در exe | ✅ `RESOURCE_ROOT` در `main.js` |
| IPCهای بلااستفاده | ✅ از `preload/main` حذف شدند |
| شماره‌ی مرحله | ✅ متمرکز در `i18n` |
| گزارش زنده‌ی نصب | ✅ `install-log` اکنون پر می‌شود |
| تصاویر tier | ✅ در کارت‌ها استفاده می‌شوند |
| تست overwrite | ✅ در `installer.test.js` اضافه شد |
| CI workflow | ⚠️ به‌صورت `docs/build-release.yml.example` (چون توکن گیت‌هاب App دسترسی workflows ندارد) |

---

## ۰. خلاصه‌ی اجرایی

پروژه از نظر **معماری، امنیت پایه، UI و موتور نصب** در وضعیت خیلی خوبی است و همه‌ی تست‌های خودکار پاس می‌شوند. اما هنوز **چند مسیر عملکردی واقعی کامل نشده** که مهم‌ترین آن‌ها عبارت‌اند از:

1. **فایل‌های واقعی مودها وجود ندارند** (و پوشه‌ها حتی در گیت ذخیره نمی‌شوند).
2. **تصمیم «بازنویسیکن / نگه‌دار» کاربر در نصب واقعاً اجرا نمی‌شود** — مهم‌ترین باگ منطقی.
3. **صفحه‌ی مدیریت مودها نسخه‌ی قدیمی را به‌جای جدید می‌خواند**.
4. **صفحه‌ی پایان «موفقیت» را حتی در حالت انصراف نشان می‌دهد**.
5. **در بیلد بسته‌بندی‌شده (exe)، خواندن فایل‌های مود از داخل asar ممکن است شکست بخورد**.

در ادامه جزئیات کامل هر مورد آمده است.

---

## ۱. مسیرهای بحرانی (blocking / باعث استفاده‌ی ناقص)

### 1.1 فایل‌های واقعی مودها غایب‌اند + پوشه‌ها در گیت نیستند ⛔
- `src/assets/mod-files/csp|pure|ppfilter|chasecam|hud|srp|video` همگی **خالی** هستند.
- خروجی `git ls-files src/assets/mod-files` **خالی** است → چون گیت پوشه‌ی خالی را ترک نمی‌کند، بعد از `git clone` این پوشه‌ها اصلاً وجود ندارند.
- نتیجه: کاربری که پروژه را کلون می‌کند، جایی برای گذاشتن فایل‌ها ندارد و نصب همه‌ی مودها «Missing» می‌شود.
- **نیاز:** افزودن `.gitkeep` داخل هر پوشه + راهنمای روشن (یا شروع به گذاشتن فایل‌های واقعی).

### 1.2 تصمیم بازنویسی CSP/PURE در نصب اعمال نمی‌شود 🐛 (مهم‌ترین باگ)
در `baseModsCheck.js` اگر کاربر روی «خیر، حفظ کن» کلیک کند، `overwriteDecisions.csp/pure = false` ذخیره می‌شود.
اما در `tierSelect.js`:
```js
const force = m.id === 'csp' || m.id === 'pure' ? !existing[m.id] : true;
return { id: m.id, ..., overwrite: force };
```
فقط یک فیلد `overwrite` تولید می‌شود که **هیچ‌جا استفاده نمی‌شود**. موتور نصب (`installer.js`) هم «overwrite» را اصلاً نمی‌خواند.
- نتیجه: اگر کاربر بگوید «خیر، حفظ کن»، برنامه باز هم فایل‌های UHM را روی مود قبلی **کپی می‌کند**.
- برای گزینه‌ی `pure` (که قابلیت merge هم دارد) به‌ویژه خطرناک است.
- **نیاز:** یا مود را از پلن نصب حذف کنیم وقتی `overwrite === false`، یا در `installer` از `mod.overwrite` استفاده کنیم (کپی فقط وقتی `overwrite !== false`).

### 1.3 صفحه‌ی مدیریت مودها، نسخه‌ی قدیمی را نشان می‌دهد 🐛
در `manageMods.js`:
```js
const latest = entries[entries.length - 1] || entries[0] || {};
```
ولی در `install.js` هر نصب جدید با `[newEntry, ...existing]` به **ابتدا** اضافه می‌شود. یعنی جدیدترین در `entries[0]` است، نه آخر.
- نتیجه: تاریخ، وضعیت و لیست فایل‌های `manageMods` مربوط به **نصب قبلی/قدیمی** است و دکمه‌ی حذف ممکن است فایل‌های اشتباه را هدف بگیرد.
- **نیاز:** `const latest = entries[0] || entries[entries.length-1]`.

### 1.4 صفحه‌ی پایان، «موفقیت» را در حالت انصراف/خطا هم نشان می‌دهد 🐛
در `done.js` همیشه `s('title')` = «نصب با موفقیت انجام شد» رندر می‌شود و `params.cancelled` استفاده نمی‌شود.
- اگر کاربر انصراف دهد یا همه‌ی مودها خطا/Missing باشند، باز «موفقیت» نشان داده می‌شود.
- **نیاز:** بر اساس `cancelled` / تعداد خطا، پیام و آیکون متفاوت (مثلاً «نصب لغو شد» / «با خطا تمام شد»).

### 1.5 مسیر فایل‌های مود در exe (بسته‌بندی‌شده) ⚠️
در `main.js`:
```js
const ASSETS_MOD_DIR = path.join(app.getAppPath(), 'src', 'assets', 'mod-files');
```
در نسخه‌ی `asar`، `app.getAppPath()` به `app.asar` اشاره می‌کند و `asarUnpack` فایل‌های مود را در `app.asar.unpacked/...` می‌گذارد. خواندن با `fs.copyFileSync` از مسیر asar ممکن است شکست بخورد (به‌خصوص در فایل‌های unpack).
- **نیاز:** helper مثل:
```js
const RESOURCE_ROOT = app.isPackaged
  ? (app.getAppPath() + '.unpacked')
  : app.getAppPath();
const ASSETS_MOD_DIR = path.join(RESOURCE_ROOT, 'src', 'assets', 'mod-files');
```
و استفاده در `install:run` و `mods:list-assets`.

---

## ۲. مسائل منطقی/UX که باید کامل شوند

- **۲.۱ «مرحلهی ویزارد» ناهماهنگ و هاردکد شده**  
  `gamePath` → `مرحله ۱ از ۵`، `tierSelect` → `مرحله ۳ از ۵`، ولی `baseModsCheck` و `install` هیچ شماره‌ای ندارند. علاوه بر آن، این رشته‌ها خارج از `i18n.js` هستند.
- **۲.۲ بخش `install-log` هرگز پر نمی‌شود**  
  در `install.js` یک `div#install-log` رندر می‌شود ولی هیچ‌جا `innerHTML/textContent` آن پر نمی‌شود.
- **۲.۳ انتخاب مودها وجود ندارد**  
  ویزارد همیشه ۷ مود را نصب می‌کند. «انتخاب مودها» در نقشه‌ی راه است ولی فعلاً پیاده نشده.
- **۲.۴ فقط سطح مود در `install` نمایش داده می‌شود**  
  پیشرفت per-file / آمار دقیق فایل‌های کپی‌شده موجود نیست (فقط `done/total` مود).
- **۲.۵ بکاپ‌های قدیمی غیرقابل مدیریت**  
  مانیفست تا ۳ نسخه می‌گیرد ولی `manageMods` فقط یک نسخه را نشان می‌دهد و راهی برای «بازگشت به نسخه‌ی قبل» نیست.
- **۲.۶ `showcase.js` دکمه‌ی مدیریت فقط بر اساس `manifest.mods` نمایش داده می‌شود** که در نصب‌های جدید درست است، ولی اگر مودها فقط `missing` باشند هم دکمه ظاهر می‌شود (چون `Array.isArray(m) && m.length > 0`). منطق قابل بهبود است.

---

## ۳. کیفیت کد و نگهداری

- **۳.۱ بدون Linter/Formatter** — فقط `node --check`. پیشنهاد `eslint` + `prettier` (اختیاری).
- **۳.۲ `MOD_DEFINITIONS.nameKey` و `TIER_DEFINITIONS.image` عملاً استفاده نمی‌شوند**؛ تصاویر `tiers/*.png` (≈9.9MB) در UI استفاده نمی‌شوند → حذف یا استفاده.
- **۳.۳ `fs:copy-with-backup` / `fs:restore-or-delete` / `fs:path-exists` کاربرد واقعی ندارند** و مسیرهای دلخواه بدون validate می‌پذیرند. اگر XSS رخ بدهد، ریسک نوشتن/حذف فایل دلخواه می‌شود. یا حذف شوند یا validate شوند.
- **۳.۴ `browser-preview.js`** حالت `__previewIdx` را بعد از هر نصب ریست نمی‌کند؛ در پیش‌نمایش دوم ممکن است پیشرفت ناهماهنگ شود.
- **۳.۵ نبود `engines` / `.nvmrc`** در `package.json`؛ توصیه‌ی Node 18.
- **۳.۶ `renderer.test.js`** فقط اسموک است؛ **کاور تصمیم overwrite، ترتیب مانیفست و حالت cancelled را پوشش نمی‌دهد.**

---

## ۴. انتشار و توزیع

| مورد | وضعیت | توضیح |
|---|---|---|
| فایل `.github/workflows/build-release.yml` | ⚠️ **کامیت محلی** | به‌خاطر نبود دسترسی `workflows` روی GitHub **push نشد** → در ریموت نیست |
| `RELEASE-GUIDE.md` | ⚠️ **untracked** | هنوز کامیت/پوش نشده |
| ساخت `exe` در سندباکس | ⛔ | دانلود باینری Electron از CDN گیتهاب در سندباکس قطع می‌شود (TLS/EOF) |
| `dist/` | ❌ | وجود ندارد |
| Code Signing | ❌ | برای توزیع عمومی لازم است |
| Auto-update | ❌ | در نقشه‌ی راه |

> نکته‌ی مهم: ریموت `arena/01a0884d-app` الان روی `c54cc3c` است، ولی کامیتِ `0d9fd75` (workflow) فقط محلی است.

---

## ۵. نکات قوی (فقط برای شفافیت)

- ✅ معماری Main / Preload / Renderer امن و صحیح
- ✅ `sandbox: true` + `contextIsolation` + whitelist API
- ✅ بکاپ/بازگردانی در موتور نصب واقعاً تست شده (تست پاس)
- ✅ اکسترکت ZIP و RAR پشتیبانی می‌شود
- ✅ فونت Vazirmatn آفلاین باندل شده
- ✅ UI، انیمیشن، Toast/Modal و RTL/LTR حرفه‌ای است
- ✅ `npm run check` هر دو تست را پاس می‌کند

---

## ۶. اولویت‌بندی پیشنهادی برای تکمیل

### 🔴 فوری (باید قبل از انتشار)
1. فیکس منطق overwrite (CSP/PURE) + سازگار کردن `tierSelect` و `installer`
2. فیکس `manageMods` برای خواندن `entries[0]`
3. فیکس `done` برای حالت انصراف/خطا
4. فیکس مسیر `asar.unpacked` در `main.js`
5. افزودن `.gitkeep` در پوشه‌های `mod-files` + کامیت `RELEASE-GUIDE.md`

### 🟡 مهم (نسخه‌ی بعدی)
6. شماره‌ی ویزارد متمرکز در `i18n`
7. تکمیل `install-log` و پیشرفت per-file
8. صفحه‌ی انتخاب مودها (فقط مودهای دلخواه نصب شوند)
9. مدیریت بکاپ‌های چند نسخه‌ای در `manageMods`

### 🟢 بهبود (اختیاری)
10. حذف/استفاده از تصاویر tier ، حذف IPCهای بلااستفاده
11. Linter/Formatter
12. Code Signing + Auto-update

---

## ۷. جمع‌بندی

پروژه در مسیر درستی است و **اسکلت، امنیت، UI و موتور نصب** آماده‌اند؛ اما برای اینکه به‌عنوان یک «نصب‌کننده‌ی واقعی و قابل انتشار» کامل شود، **حداقل ۵ مورد بحرانی** (بند ۱) هنوز باز است. مهم‌ترین آن‌ها:
- فایل‌های واقعی مودها و `.gitkeep`
- عدم اعمال تصمیم «بازنویسی/نگه‌دار»
- ترتیب اشتباه نسخه‌ها در مدیریت مودها
- پیام‌های نادرست صفحه‌ی پایان در انصراف/خطا
- سازگاری مسیر فایل‌ها در بیلد `asar`

اگر بخواهی، همین الان می‌توانم **۵ مورد بحرانی** را اصلاح و تست کنم (و یک پرونده‌ی `test` برای تصمیم overwrite اضافه کنم). شروع کنم؟
