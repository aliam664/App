# 🐞 گزارش جامع باگ‌یابی UHM Pack Installer

> تاریخ: 2026-09-10 · شاخه: `arena/01a08ca6-app`
> این گزارش نتیجه‌ی بررسی تک‌تک بخش‌های پروژه (main/preload/lib/pages/css) است؛ بخش «عملکرد/فریم» در صدر است چون شکایت اصلی بود.

---

## 🖼 چرا عکس بعضی از مادها در کتابخانه نمایش داده نمی‌شود؟ (علت‌یابی دقیق)

این مشکل **یک علت واحد نداشت**؛ چهار علت مستقل با هم ترکیب شده بودند که هرکدام به‌تنهایی می‌توانست عکسِ یک ماد را حذف کند:

| # | علت ریشه‌ای | توضیح | وضعیت |
|---|---|---|---|
| 1 | **سقف ۹۰۰ کیلوبایتی** در `previewToDataUrl` | هر پیش‌نمایش بزرگ‌تر از ۹۰۰KB به‌جای تصویر، `{ tooLarge: true }` برمی‌گرداند و کارت خاکستری می‌ماند. بیشتر پیش‌نمایش‌های باکیفیت مادها (۱۰۲۴×۵۷۶ به بالا) از این سقف رد می‌شدند. | ✅ رفع شد — دیگر از خواندن خام فایل استفاده نمی‌شود؛ `nativeImage` در main process تصویر را تا ۶۴۰px کوچک می‌کند |
| 2 | **فقط `ui/preview.*` بررسی می‌شد** | خیلی از مادها عکس سراسری ندارند؛ عکسشان فقط داخل `skins/<name>/preview.jpg` است. چون این مسیر هرگز چک نمی‌شد، `hasPreview=false` می‌شد. | ✅ رفع شد — تابع جدید `findCarPreview` در نبود `ui/preview.*` اولین پیش‌نمایش اسکین را برمی‌گرداند |
| 3 | **پسوند `.jpeg` جا افتاده بود** | لیست کاندیداها فقط `.png` و `.jpg` داشت؛ مادهایی که `ui/preview.jpeg` داشتند دیده نمی‌شدند. | ✅ رفع شد — `.jpeg` به هر دو لیست ماشین/مپ اضافه شد |
| 4 | **MIME پسوندهای ناشناخته اشتباه بود** | `.webp` و `.bmp` با `image/png` برچسب می‌خوردند و مرورگر نمی‌توانست decode کند. | ✅ رفع شد — نگاشت درست `image/webp` و `image/bmp` |

**علاوه بر این‌ها** یک باگ جانبی هم پیدا و رفع شد: بعد از تعویض تب/جستجو/مرتب‌سازی، `hydratePreviews` دیگر صدا زده نمی‌شد و همه‌ی کارت‌ها (حتی آن‌هایی که عکس داشتند) خاکستری می‌ماندند؛ و سنتینل `TOO_LARGE` در کش، در رندر بعدی به‌عنوان URL تصویر استفاده می‌شد.

---

## 🚀 ۱) چرا فریم پایین است؟ (علت‌یابی عملکرد)

مشکل «لگ و فریم پایین هنگام چرخیدن داخل منوی اپ» یک علت واحد ندارد؛ ترکیبی از چند تصمیم است که روی هم رفته رندر را خیلی سنگین می‌کند:

| # | علت | تأثیر | وضعیت |
|---|---|---|---|
| 1 | `app.disableHardwareAcceleration()` در `main.js` | **بزرگ‌ترین علت.** کل UI را مجبور به رندر نرم‌افزاری (CPU) می‌کرد. یعنی تمام blur/سایه/انیمیشن/ترنزیشن با CPU رسم می‌شدند و فریم می‌افتاد. | ✅ رفع شد |
| 2 | `backdrop-filter: blur(...)` روی تقریباً **هر کارت، دکمه، تب و input** (۲۸ مورد) | blur پس‌زمینه گران‌ترین افکت CSS است؛ با رندر نرم‌افزاری، اسکرول/جا‌به‌جایی صفحات بسیار سنگین می‌شد. | ✅ حذف شد (فقط روی titlebar و toast/modal که معدود و موقت هستند نگه داشته شد) |
| 3 | `filter: blur(24px) …` و `blur(20px)` روی تصاویر پس‌زمینه‌ی اسلایدر هوم/شوکیس | هر فریم یک تصویر بزرگ دوباره فیلتر و rasterize می‌شد؛ هنگام اسلایدشو و اسکرول افت محسوس می‌داد. | ✅ حذف شد (تیره‌شدن با گرادیان اورلی حفظ شد) |
| 4 | شفافیت بیش از حد کارت‌ها (`--color-card-bg` شب = سفید ۵.۵٪) | بدون blur کارت‌ها تقریباً نامرئی می‌شدند؛ پس زمینه را به شیشه‌ی تیره‌ی کدر تبدیل کردم تا بدون blur هم خوانا بماند. | ✅ تنظیم شد |
| 5 | `transparent: true` + `frame: false` | هزینه‌ی کامپوزیت شفافیت؛ با برگشت شتاب سخت‌افزاری قابل تحمل است. | ⚠️ نگه داشته شد (جزو طراحی است) |
| 6 | اسلایدهای غیرفعال اسلایدر همچنان کامپوزیت می‌شدند (`opacity:0` ولی بدون `visibility`) | ✅ با `visibility:hidden/visible` اصلاح شد |
| 7 | `detectSystemSpecs()` در هر ورود به هوم و تنظیمات، `nvidia-smi` + `powershell.exe` + `wmic` را با timeoutهای ۳ ثانیه spawn می‌کرد | بار CPU اضافه در لحظه‌ی رندر + کندی اولین پاسخ | ✅ در main کش شد (فقط با `force` دوباره اجرا می‌شود) |
| 8 | اسکن کتابخانه (`scanLibrary` + `walkStats` بازگشتی روی همه‌ی فایل‌ها) **همگام** در main process | هنگام باز کردن کتابخانه، main را موقتاً مسدود می‌کند | ⚠️ گزارش شد (نیاز به async/chunking دارد — تغییر رفتار بزرگ) |
| 9 | آدرس‌های `data:base64` برای پیش‌نمایش کتابخانه به‌صورت سریالی از IPC | کندی بارگذاری تصاویر کتابخانه + مصرف حافظه | ✅ تا حدی (کش + بارگذاری در هر render) |

**نتیجه:** پس از اصلاحات ۱ تا ۴، رندر روی GPU برگشته و سنگین‌ترین افکت‌های هر-فریم حذف شده‌اند؛ انتظار می‌رود فریم هنگام گردش در منو به‌وضوح بهتر شود.

---

## 🔴 ۲) باگ‌های عملکردی مهم (پیدا شده)

### ۲-۱) انتخاب «سطح سیستم» هیچ اثری نداشت — همیشه هر ۷ مود نصب می‌شد ⭐
- **فایل:** `src/pages/tierSelect.js` → `buildPlan(tier)`
- `tier` در ساخت پلن **استفاده نمی‌شد**؛ صرفاً همه‌ی مودهای `enabled` وارد لیست می‌شدند. یعنی «Low» دقیقاً همان چیزی را نصب می‌کرد که «Ultra».
- **اثبات:** برای هر ۵ سطح خروجی یکسان بود: `csp, pure, ppfilter, chasecam, hud, srp, video`.
- ✅ **رفع شد:** حالا `TIER_MODS[tier]` فیلتر می‌شود (Low = ۳ مود … Ultra = ۷ مود).

### ۲-۲) صفحه‌ی «درباره ما» — بخش «داخل پک چیست؟» اسم و توضیح مودها خالی بود
- **فایل:** `src/pages/about.js` + `src/js/modConfig.js`
- `MOD_DEFINITIONS[].nameKey` مقدار `'csp'` داشت ولی کلید واقعی i18n `showcase.modCsp` است → `t()` رشته‌ی خالی برمی‌گرداند.
- توضیح `ppfilter` و `chasecam` هم با `modPpfilterDesc`/`modChasecamDesc` (کلید ناموجود) ساخته می‌شد.
- ✅ **رفع شد:** کلیدهای `nameKey`/`descKey` با کلیدهای واقعی i18n هماهنگ شدند.

### ۲-۳) VRAM هیچ‌وقت در پیشنهاد سطح استفاده نمی‌شد
- **فایل:** `src/lib/hardware.js` → `detectSystemSpecs()`
- `gpuVramGb` تشخیص داده می‌شد ولی به `suggestTierFromSpecs(...)` **پاس داده نمی‌شد**؛ بنابراین مسیر fallback «GPU ناشناخته → تخمین با VRAM» مرده بود و برای GPUهای ناشناخته پیشنهاد اشتباه (بر اساس CPU/RAM) داده می‌شد.
- ✅ **رفع شد.**

### ۲-۴) کتابخانه: پیش‌نمایش‌ها بعد از تعویض تب/جستجو/مرتب‌سازی بارگذاری نمی‌شدند
- **فایل:** `src/pages/library.js`
- `hydratePreviews()` فقط یک‌بار بعد از اسکن صدا زده می‌شد؛ هر `renderGrid()` (تب، جستجو، مرتب‌سازی) گرید را از نو می‌ساخت ولی دیگر هیدریت نمی‌کرد → کارت‌ها خاکستری/بدون عکس می‌ماندند.
- ✅ **رفع شد:** هیدریت به انتهای `renderGrid` منتقل شد + کش per-path تا IPC تکراری نزنیم.

### ۲-۵) کتابخانه: state کهنه بین ورودها → ناهماهنگی UI و داده
- **فایل:** `src/pages/library.js` → `render()`
- تب/جستجو/مرتب‌سازی/فیلترها بین مراجعه‌های مجدد ریست نمی‌شدند؛ مثلاً اگر با تب «حذف‌شده‌ها» خارج می‌شدید، ورود بعدی دکمه‌ی «همه» فعال نشان داده می‌شد ولی گرید سطل‌زباله رندر می‌شد.
- ✅ **رفع شد:** state موقت در ابتدای `render()` ریست می‌شود.

### ۲-۶) برچسب دکمه‌ی «خالی کردن سطل» در کتابخانه اشتباه بود
- دکمه‌ی خالی‌کردن سطل از کلید `library.emptyTrash` («سطل بازیافت خالی است») استفاده می‌کرد؛ یعنی روی دکمه‌ی عمل، متنِ وضعیت چاپ می‌شد.
- ✅ **رفع شد:** کلید جدید `library.emptyTrashAction` اضافه شد.

### ۲-۷) برچسب آمار «Preview» انگلیسی هاردکد بود
- ✅ **رفع شد:** کلید `library.statsPreviews` جایگزین شد.

### ۲-۸) اعتبارسنجی مسیر بازی روی هر کاراکتر IPC می‌زد
- `src/pages/gamePath.js`: رویداد `input` بدون debounce به ازای هر حرف `validateGamePath` صدا می‌زد.
- ✅ **رفع شد:** debounce ۲۵۰ms اضافه شد.

### ۲-۹) نصب از آرشیو RAR هیچ فایلی استخراج نمی‌کرد + پشتیبانی رمز ⭐
- **فایل:** `src/lib/modInstaller.js`
- `extractToStaging` برای RAR از `extractor.extract({ files: [] })` استفاده می‌کرد؛ در `node-unrar-js` آرایه‌ی خالی یعنی «هیچ‌چیز استخراج نکن» → استیج خالی می‌ماند و هیچ فایلی کپی نمی‌شد. بنابراین نصب مود از فایل `.rar` عملاً بی‌اثر بود.
- ✅ **رفع شد:** با حذف گزینه‌ی `files` (استخراج همه) و پیمایش کامل iterator، استخراج واقعی انجام می‌شود.
- **پشتیبانی رمز (جدید):** آرشیوهای رمزدار حالا در تحلیل شناسایی می‌شوند (`header-encrypted` از خطای `ERAR_MISSING_PASSWORD` هنگام فهرست‌خوانی، و `file-encrypted` از فلگ `flags.encrypted` هر ورودی). اگر رمز لازم باشد صفحه‌ی درخواست رمز نشان داده می‌شود؛ رمز اشتباه → `PASSWORD_INCORRECT` با امکان تلاش مجدد، و رمز درست در تحلیل و نصب استفاده و یک‌بار با استخراج کوچک‌ترین فایل رمزدار اعتبارسنجی می‌شود.

---

## 🟡 ۳) موارد پیدا شده ولی عمداً دست‌نخورده (پیشنهادی)

> ✅ = در همین بازبینی حرفه‌ای رفع شد · ⚠️ = همچنان باز

| # | موضوع | توضیح | وضعیت |
|---|---|---|---|
| 1 | اسکن کتابخانه همگام بود | `scanLibrary`/`walkStats` در main process همه‌ی فایل‌ها را synchronous پیمایش می‌کرد؛ در کتابخانه‌های بزرگ پنجره منجمد می‌شد. | ✅ رفع شد — تبدیل به async با `fs.promises` و yield هر ۱۲۸ ورودی + پیشرفت زنده (`library:scan-progress`) |
| 2 | قوانین GPU در `hardware.js` هم‌پوشانی دارند | RX 6700 XT / 6800 XT / 6900 XT اول با regex «ultra» مچ می‌شوند و ردیف‌های `veryhigh` مربوطه dead هستند. | ⚠️ طبقه‌بندی GPU یک تصمیم محصول است (تست‌ها هم «ultra» را تأیید می‌کنند)؛ ردیف‌های تکراری بی‌ضررند و دست نمی‌خورند. |
| 3 | `parseVramToGb` و AdapterRAM | وقتی PowerShell مقدار UINT32_MAX (۰xFFFFFFFF) برگرداند، به‌اشتباه ۴GB گزارش می‌شد. | ✅ رفع شد — sentinel به‌صورت «نامشخص» (null) برگردانده می‌شود؛ واحد (MiB/bytes) هم حالا صریح از هر منبع پاس داده می‌شود. |
| 4 | `filterLibrary` در `src/lib/library.js` | شرط `onlyMods` با ternary تو در تو گیج‌کننده بود. | ✅ رفع شد — به `if (onlyMods && !item.isMod) return false;` ساده شد. |
| 5 | `previewToDataUrl` برای پسوندهای ناشناخته | مثلاً `.webp` با mime `image/png` برمی‌گشت. | ✅ رفع شد — نگاشت `image/webp` و `image/bmp` اضافه شد. |
| 6 | `doRestore` آیتم بازیابی‌شده را به لیست محتوا اضافه نمی‌کرد | بعد از بازیابی، آیتم فقط از trash حذف می‌شد و در تب‌های محتوا دیده نمی‌شد. | ✅ رفع شد — بعد از بازیابی، trash و محتوا هر دو refresh می‌شوند. |
| 7 | دکمه‌ی «نمایش پوشه» (reveal) واقعاً کاری نمی‌کرد | `doReveal` فقط toast می‌داد و دکمه‌ای هم به آن وصل نبود. | ✅ رفع شد — IPC جدید `library:reveal` + `shell.showItemInFolder` با گارد path-traversal، دکمه در کارت/لیست/جزئیات. |
| 8 | کش سخت‌افزار بعد از اولین تشخیص ثابت می‌ماند | قابل قبول است (سخت‌افزار در runtime عوض نمی‌شود) ولی اگر GPU اکسترنال قطع/وصل شود به‌روز نمی‌شود. | ⚠️ دکمه‌ی «تشخیص مجدد» در تنظیمات `force` می‌فرستد — کافی است. |

---

## ✅ ۴) خلاصه‌ی اصلاحات انجام‌شده

| فایل | تغییر |
|---|---|
| `main.js` | فعال‌کردن دوباره‌ی شتاب سخت‌افزاری + کش نتیجه‌ی `system:detect-specs` با گزینه‌ی `force` |
| `preload.js` | عبور options به `detectSystemSpecs` |
| `src/lib/hardware.js` | پاس‌دادن `gpuVramGb` به `suggestTierFromSpecs` |
| `src/pages/tierSelect.js` | `buildPlan` حالا واقعاً از `TIER_MODS[tier]` استفاده می‌کند |
| `src/js/modConfig.js` | هماهنگ‌سازی `nameKey`/`descKey` با کلیدهای i18n |
| `src/pages/about.js` | استفاده از `descKey` برای توضیح مودها |
| `src/pages/settings.js` | دکمه‌ی «تشخیص مجدد» force می‌زند؛ رندر عادی از کش استفاده می‌کند |
| `src/pages/gamePath.js` | debounce اعتبارسنجی مسیر + گارد race با توکن توالی (پاسخ کهنه پاسخ جدید را بازنویسی نمی‌کند) |
| `src/pages/library.js` | ریست state، هیدریت پیش‌نمایش در هر render + کش، برچسب‌های درست، کلیک کارت گرید → جزئیات، هیدریت موازی با همزمانی محدود (۸تایی) + dedup درخواست‌های در حال پرواز، رفع باگ سنتینل `TOO_LARGE` |
| `src/js/i18n.js` | کلیدهای `emptyTrashAction` و `statsPreviews` (fa/en) |
| `src/css/base.css` | حذف backdrop-filter از عناصر تکراری (فقط titlebar/toast/modal)، پس‌زمینه‌ی کارت‌ها کدرتر |
| `src/css/pages.css` | حذف blur زنده‌ی اسلایدها، `visibility` برای اسلایدهای غیرفعال، حذف backdrop-filter |
| `src/css/library.css` | حذف backdrop-filter از کارت/تب/جستجو/فیلتر + استایل نوار پیشرفت اسکن |
| `src/lib/library.js` | اسکن async (cars/tracks) + `findCarPreview` (fallback اسکین) + کاندیداهای `.jpeg` + MIME صحیح + ساده‌سازی `filterLibrary` |
| `main.js` | `library:scan` async با emit پیشرفت، `makePreviewDataUrl` (nativeImage تا ۶۴۰px)، IPC `library:reveal` |
| `preload.js` | اکسپوز `revealContent` و `onScanProgress` |
| `src/js/modConfig.js` | منبع واحد متادیتای مودها (`MOD_LABEL` / `MOD_LABEL_SHORT` / `MOD_ICON` / `TIER_MODS`) |
| `src/pages/showcase.js`, `tierSelect.js`, `install.js`, `done.js`, `manageMods.js` | حذف تعریف‌های تکراری متادیتا و ارجاع به `window.*` |
| `src/lib/hardware.js` | `parseVramToGb(raw, unit)` با واحد صریح + sentinel 0xFFFFFFFF + `specs.constrained` + گارد خطا در تشخیص GPU |
| `src/js/ui.js` | escape کردن label/value در `infoRow` و `statCard` (جلوگیری از تزریق HTML) |
| `test/hardware.test.js`, `test/library.test.js` | تست‌های جدید برای واحد VRAM، sentinel، و fallback پیش‌نمایش اسکین |
| `src/lib/modInstaller.js` | پشتیبانی RAR رمزدار (تشخیص header/file-encrypted، اعتبارسنجی رمز، نصب با رمز) + رفع باگ استخراج کامل RAR (`extract({files:[]})` هیچ فایلی استخراج نمی‌کرد) |
| `main.js`, `preload.js`, `src/pages/modInstall.js` | عبور `password` در IPC و ویزارد؛ صفحه‌ی درخواست رمز با retry و پیام «رمز اشتباه» |
| `src/js/i18n.js` | کلیدهای `passwordTitle/passwordSub/passwordPlaceholder/passwordSubmit/passwordIncorrect` (fa/en) |
| `test/modInstaller.test.js` + `test/fixtures/*.rar` | تست‌های RAR ساده/رمزدار (header/file encryption) و نصب end-to-end با فیکسچرهای واقعی |

---

## 🟣 ۶) بازبینی دوم — باگ‌های تازه پیدا و رفع‌شده

دور دوم بررسیِ کل پروژه (main/preload/pages/lib/test) این موارد را پیدا و اصلاح کرد:

| # | باگ | جزئیات | وضعیت |
|---|---|---|---|
| 6-1 | **رمزِ «per-file» در RAR همیشه `PASSWORD_INCORRECT` می‌شد** ⭐ | `verifyRarPassword` فقط **کوچک‌ترین** فایل رمزدار را امتحان می‌کرد. در آرشیوهای per-file-encrypted هر ورودی رمز خودش را دارد، پس رمزِ درستِ یک فایل (مثلاً `2中文` برای `FileEncByName.rar`) روی فایلِ کوچک‌ترِ دیگری (با رمز `3Sec`) fail می‌شد و تحلیل اشتباهاً «رمز غلط» گزارش می‌کرد. | ✅ رفع شد — رمز روی **همه‌ی** ورودی‌های رمزدار (کوچک‌به‌بزرگ) امتحان و در صورت بازکردنِ هر کدام پذیرفته می‌شود |
| 6-2 | **استخراج RAR در installer قدیمی (`installMods`) هیچ فایلی استخراج نمی‌کرد** | همان باگ `extract({ files: [] })` که قبلاً در `modInstaller.js` رفع شده بود در `src/lib/installer.js` (`extractArchive`) باقی مانده بود؛ مودهای `type: 'extract'` با پسوند `.rar/.cbr` عملاً خالی می‌ماندند و `MISSING_FILES` می‌شدند. | ✅ رفع شد — `extract()` بدون گزینه‌ی `files` + پیمایش کامل iterator |
| 6-3 | **نصبِ ظاهراً موفق با صفر فایل کپی‌شده** | در `executeInstall` اگر فایل‌های مبدأ روی دیسک پیدا نمی‌شدند، آیتم باز هم `installed` علامت می‌خورد (موفقیتِ دروغینِ بی‌صدا). | ✅ رفع شد — در نبودِ هیچ فایل کپی‌شده، وضعیت `error` با کد `FILES_MISSING` ثبت می‌شود |
| 6-4 | **درگ چند فایل با هم → فقط آرشیو اول نصب می‌شد** | `startInstall` همیشه `state.sources[0]` را نصب می‌کرد؛ آیتم‌های آرشیو دوم به بعد در review دیده می‌شدند ولی بی‌صدا جا می‌ماندند. | ✅ رفع شد — آیتم‌ها بر اساس منبعشان گروه‌بندی و هر آرشیو جداگانه نصب می‌شود؛ نتیجه تجمیع و نمایش داده می‌شود |
| 6-5 | **دو بار IPC برای اعتبارسنجی مسیر بازی در لود اولیه** | `render()` و `wire()` هر دو `validateAndRender` را با مسیر از قبل تنظیم‌شده صدا می‌زدند (یک round-trip اضافه). | ✅ رفع شد — فراخوانی تکراری حذف شد |
| 6-6 | **آیتم‌های لیست نصب ممکن بود ردیفِ اشتباه را آپدیت کنند** | `handleProgress` ایندکس ردیف را از ترتیبِ رسیدن نتیجه (نه ترتیب پلن) می‌گرفت؛ با پیشرفتِ خارج‌از‌ترتیب یا تکراری، وضعیت روی ردیف غلط می‌نشست. | ✅ رفع شد — نگاشت ثابت `mod id → ردیف` از خود پلن ساخته می‌شود |
| 6-7 | **اسکنِ تکراری و گرانِ conflict در `mods:install`** | `buildPlan(gamePath, …)` قبل از نصب، برای آیتم‌های `mirror` کل پوشه‌ی بازی را **همگام** فهرست می‌کرد در حالی که `executeInstall` خودش مقصد را از type/name دوباره می‌سازد. | ✅ رفع شد — فقط `sanitizeInstallItems` اجرا می‌شود (اسکن بیهوده حذف) |
| 6-8 | **نصبِ نیمه‌موفق در manifest ثبت نمی‌شد** | رکورد `manifest.installs` فقط وقتی نوشته می‌شد که `result.success` بود؛ اگر یک آیتم از چند آیتم خطا می‌خورد، آیتم‌های موفق هم ثبت نمی‌شدند. | ✅ رفع شد — هر آیتم `installed` حتی در batch نیمه‌موفق ثبت می‌شود |
| 6-9 | **حذف/بازیابی از سطل بازیافت main process را فریز می‌کرد** | `moveToTrash`/`restoreTrashItem`/`emptyTrash`/`permanentlyDeleteTrashItem` از `fs.cpSync`/`fs.rmSync` استفاده می‌کردند؛ کپی/حذف یک پوشه‌ی بزرگ (مثلاً یک ماشین چندصد مگابایتی) کل پنجره را چند ثانیه منجمد می‌کرد. | ✅ رفع شد — تبدیل به `fs.promises.cp/rm` (async)؛ main process دیگر هنگام حذف محتوا قفل نمی‌شود |

**تست‌های جدید اضافه‌شده:** `test/modInstaller.test.js` (رمز per-file صحیح `2中文`/`3Sec` پذیرفته و رمز غلط رد می‌شود؛ `verifyRarPassword` مستقیم تست شد؛ `executeInstall` با فایل‌های گم‌شده → `FILES_MISSING`) و `test/installer.test.js` (استخراج واقعی RAR با مسیرهای space/یونیکد از `FolderTest.rar`).

---

## 🧪 ۵) وضعیت تست‌ها

```
node scripts/check.js            ✔ (syntax + assets + همه‌ی تست‌ها)
test/installer.test.js           ✔ PASSED
test/hardware.test.js            ✔ PASSED
test/renderer.test.js            ✔ PASSED
test/library.test.js             ✔ PASSED
test/libraryData.test.js         ✔ PASSED
test/modInstaller.test.js        ✔ PASSED
```

> برای اجرای تست‌های `installer` و `renderer` وابستگی‌ها (`adm-zip`, `node-unrar-js`, `linkedom`) لازم است. در این سندباکس npm به‌دلیل TLS-intercept شبکه نمی‌توانست نصب کند، بنابراین بسته‌ها به‌صورت دستی از رجیستری (که مستقیم با Node در دسترس بود) وندور و نصب شدند و هر ۵ تست سبز هستند.
